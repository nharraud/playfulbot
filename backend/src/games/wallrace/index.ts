import init from './init';
import * as gameStateSchema from './gameStateSchema.json';
import actions from './actions';

const minPlayers = 2;
const maxPlayers = 2;

export { minPlayers, maxPlayers, gameStateSchema, init, actions };
