query authenticatedUserTournaments {
  authenticatedUser {
    id,
    username,
    teams {
      id,
      name,
      tournament {
        id,
        name,
        lastRoundDate,
        status,
      }
    }
  }
}